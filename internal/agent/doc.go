// Package agent holds the built-in agent registry (spec §5.7).
//
// It maps an agent name to its default image, install command, environment, and
// default template tag. v1 ships exactly one entry (opencode). No logic is
// implemented yet.
package agent
